import Transaction from '../models/transaction.model';

export const createTransactionsFromCSV = (file) => {
    return [new Transaction()];
};

export const createTransaction = (transaction) => {
    
};

